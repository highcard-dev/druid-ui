---
"@druid-ui/plattform": major
---

Retire the standalone `@druid-ui/config-editor` package and remove the
`saveFileToDeploymentIfMatch` platform API. Move configuration-editor
integrations to the inline editor shipped with Druid Scrolls.
